import { PuffLoader } from 'react-spinners';

export const loader = (
  <div className="flex items-center justify-center py-[40vh]">
    <PuffLoader color="#c70039" size={70} speedMultiplier={1} />
  </div>
);
